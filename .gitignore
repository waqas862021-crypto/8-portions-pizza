# Secrets
.env

# Runtime data generated by backend/server.ps1 - accumulates real
# customer PII (names, phone numbers, delivery addresses) once orders
# are confirmed, so it must never be committed. The backend creates this
# file itself on first confirmed order if it's missing.
data/orders.json

# Python
__pycache__/
*.pyc
venv/
.venv/

# Node
node_modules/
npm-debug.log*

# Editor/OS
.vscode/
.DS_Store
.claude/

# Generated scratch output (e.g. downloadable scaffold zips), not source
Claude outputs/
