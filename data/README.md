# data/

Put static or sample data here, for example:

- `menu.json` — the pizza menu (names, sizes, prices, toppings)
- `sample_orders.json` — example orders for testing

Start with simple flat files (JSON/CSV). Only move to a real database
later if the project actually needs one — that keeps costs and
complexity low while you're learning.

No data files have been added yet.
